# rghoshal.github.io
This repo will contain articles on engineering (system design , machine learning and deep learning ) of my choice.

https://rghoshal.github.io/

# 1. Login to GitHub (if not already logged in)
git config --global user.name "Your Name"
git config --global user.email "[EMAIL_ADDRESS]"

# 2. Create a new repository (if you haven't already)
#    Run this in a new folder called "docs" or inside your project folder
git init
git branch -M main

# 3. Add your files
git add .

# 4. Commit the changes
git commit -m "Updates to index.md"

# 5. Push to GitHub
git remote add origin <YOUR_REPO_URL>
git push -u origin main

Shortcuts

All these commands from /Document/rghoshal.github.io directory

git add .
git commit -m "Updates to index.md"
git pull --rebase origin main  
git push origin main
mkdocs gh-deploy --force

Actual Github site :
If current deployment not working , get a new deployment to start in
Github.

